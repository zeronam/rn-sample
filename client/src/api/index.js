export const create = (params) => {    
    const dataList = {
        data: {data1: 1, data2: 2,},
        statusCode: 200,
    }
        return dataList;
};
    
export const commentProject = (params) => {    
    const commentList = {
        comment: {comment1: 1, comment2: 2,},
        statusCode: 200,
    }
        return commentList;
};